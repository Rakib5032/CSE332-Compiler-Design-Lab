printf("%s ", token);
        // char val_token[10];
        // strcpy(val_token, token);
        // char *next = strtok(NULL, " ,;\n");
        // if(next && next[0] == '['){
        //     strcat(val_token, next);
        // }
        // else if(next){
        //     token = next;
        // }
        // else{
        //     token = NULL;
        // }
